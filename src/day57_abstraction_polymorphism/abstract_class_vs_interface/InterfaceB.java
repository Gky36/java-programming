package day57_abstraction_polymorphism.abstract_class_vs_interface;

public interface InterfaceB {
}

/**
 * MARKER INTERFACE ->
 * interface without any methods or variables. We can use this for polymorphism or
 */

/*
When to go for interface:
    ->  Purpose of interface is to store abstract methods so that sub classes can implement
    -> When we need support for multiple inheritance, we go for interface
    -> When we need some simple abstraction without support for constructor, instance variables
    -> OTHER benefit pf abstraction is polymorphism
 */